/**
 * Các hằng / type dùng chung (backend, bot, frontend).
 * Có thể chuyển sang TypeScript (.ts) khi dự án dùng TS.
 */

export const API_VERSION = '1.0.0';

export const ROUTES = {
  HEALTH: '/health',
  API_INFO: '/api/info',
};
