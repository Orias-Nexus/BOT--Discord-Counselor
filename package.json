{
  "name": "my-discord-system",
  "version": "1.0.0",
  "private": true,
  "description": "Discord Assistant: Directive (Bot) + Backend API + Web Dashboard",
  "scripts": {
    "dev:directive": "cd directive && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "deploy:directive": "cd directive && npm run deploy",
    "install:all": "cd directive && npm ci && cd ../backend && npm ci && cd ../frontend && npm ci"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
